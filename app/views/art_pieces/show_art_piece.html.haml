- content_for :top do
  = render 'partials/navigation'

%main.l-container
  .art-piece
    .art-piece__holder
      = image_tag(@art_piece.image_url, class: 'art-piece__image')

    %h2.art-piece__title= @art_piece.title
    %small.art-piece__author= @art_piece.author

    .actions
      = link_to '👎', rate_art_piece_path(id: @art_piece.id, type: 'dislike'),
        class: 'actions__link actions__link--dislike'
      = link_to '👍', rate_art_piece_path(id: @art_piece.id, type: 'like'),
        class: 'actions__link actions__link--like'
